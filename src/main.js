/**
 * 
 * Remotely Executable
 * Network Call
 * Network Address
 * http://localhost:4000/
 */
import express from "express";
const app = express();
app.listen(4000);


function main(){
  return "Hello World";
}